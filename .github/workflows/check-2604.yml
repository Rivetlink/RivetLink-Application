name: check-2604

# Throwaway probe: does GitHub host an ubuntu-26.04 runner yet? Run manually.
on: workflow_dispatch

jobs:
  check:
    runs-on: ubuntu-26.04
    steps:
      - run: cat /etc/os-release
